import iterator from Std\Set in
let build n acc = if n <= 0 then acc else build (n - 1) (n :: acc) in
let nums = build 100 [] in
let s = {x for x = nums} in
let iter = iterator s in
foldl (\acc x -> acc + x) 0 iter
