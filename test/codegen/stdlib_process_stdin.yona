import spawn, writeStdin, closeStdin, readAll, wait from Std\Process in
let proc = spawn "cat" in
let _ = writeStdin proc "hello from stdin" in
let _ = closeStdin proc in
let out = readAll proc in
let _ = wait proc in
out
